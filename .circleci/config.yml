version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7
    working_directory: ~/dogfighter
    steps:
      - checkout
      - run:
          command: echo "hi"

  deploy:
    working_directory: ~/dogfighter
    docker:
      - image: node:6.10
    steps:
      - checkout
      - run:
          name: Install AWS CLI
          command:
            sudo apt-get -y -qq install awscli
      - run:
          name: Deploy to S3
          command: |
            aws --region us-west-2 s3 sync ./ s3://dogfighter-deployments/releases --delete

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy
